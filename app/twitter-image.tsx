// Twitter card: re-export the OG image. Twitter accepts the same 1200×630
// `summary_large_image` format, so we don't need a separate render path.
export { default, alt, size, contentType } from "./opengraph-image";
